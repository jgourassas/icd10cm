<h1>New Icd10clinical</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.icd10clinical_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.icd10clinical_path(@conn, :index) %></span>
